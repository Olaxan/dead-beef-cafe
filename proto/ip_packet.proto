syntax = "proto3";

package ip;

enum Protocol
{
	TCP = 0;
	UDP = 1;
	ICMP = 2;
}

message IpPackage
{
	bytes src_ip = 1;
	bytes dest_ip = 2;
	int32 src_port = 3;
	int32 dest_port = 4;
	Protocol protocol = 5;
	string payload = 6;
}